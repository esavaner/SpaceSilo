{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": [],
      "inputs": [
        "$TURBO_DEFAULT$",
        "{projectDir}/eslint.config.*",
        "{projectDir}/.eslintrc.*",
        "{workspaceRoot}/apps/shared/eslint.config.mjs",
        "{workspaceRoot}/.eslintignore",
        "{workspaceRoot}/.prettierrc"
      ],
      "dependsOn": ["^lint"]
    },
    "typecheck": {
      "outputs": [],
      "inputs": [
        "$TURBO_DEFAULT$",
        "{projectDir}/tsconfig.json",
        "{projectDir}/tsconfig.*.json",
        "{projectDir}/next-env.d.ts",
        "{projectDir}/expo-env.d.ts",
        "{projectDir}/nativewind-env.d.ts",
        "{workspaceRoot}/apps/shared/dist/**"
      ],
      "dependsOn": ["^build"]
    },
    "check": {
      "outputs": [],
      "dependsOn": ["^check"]
    },
    "reset": {
      "cache": false
    },
    "clean": {
      "cache": false
    }
  }
}
